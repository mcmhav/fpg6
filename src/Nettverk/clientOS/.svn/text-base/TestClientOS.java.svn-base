package clientOS;

import java.util.ArrayList;
import java.util.Date;
import objectTypes.Employee;
import objectTypes.Event;
import objectTypes.Meeting;
import objectTypes.Message;
import objectTypes.Room;
import objectTypes.TimeFrame;
public class TestClientOS {
	public static void main(String[] args) {
		ClientOS c = new ClientOS(6789, "127.0.0.1");
		ClientService cs = new ClientService(c);

		
//		ArrayList<Event> events = cs.getAllEvents(employee);
//		System.out.println(events.get(0).getTime().getStart().toString());
	
	}
}
